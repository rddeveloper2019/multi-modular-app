name: login
resolution: workspace
description: "A new Flutter package project."
version: 0.0.1
homepage:

environment:
  sdk: ^3.11.3
  flutter: ">=3.0.0 <4.0.0"

dependencies:
  datastore:
    path: "../../../core/datastore"

  flutter:
    sdk: flutter
  dio: 5.9.2
  retrofit: ^4.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  retrofit_generator: ^10.2.5

flutter:
